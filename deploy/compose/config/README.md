# Configuration Files

This directory stores the default Rhythm Arena configuration files used by
[docker-compose.yml](../docker-compose.yml).

Update the example secrets, domains, and service credentials before starting a
deployment. Additional examples are available in the repository's
[/config/](../../../config/) directory.
