apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "ret2shell.platformName" . }}-smoke-ping
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ret2shell.commonLabels" . | nindent 4 }}
    app.kubernetes.io/component: tests
  annotations:
    helm.sh/hook: test
spec:
  template:
    spec:
      restartPolicy: Never
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
        - name: smoke-ping
          image: {{ include "ret2shell.testImage" . }}
          imagePullPolicy: {{ .Values.global.testImage.pullPolicy }}
          command:
            - sh
            - -ec
            - curl -fsS http://{{ include "ret2shell.platformName" . }}:{{ .Values.platform.service.port }}/api/ping

{{- if eq .Values.victoriaLogs.mode "internal" }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "ret2shell.victoriaLogsName" . }}-smoke
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ret2shell.commonLabels" . | nindent 4 }}
    app.kubernetes.io/component: tests
  annotations:
    helm.sh/hook: test
spec:
  template:
    spec:
      restartPolicy: Never
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
        - name: smoke-victoria-logs
          image: {{ include "ret2shell.testImage" . }}
          imagePullPolicy: {{ .Values.global.testImage.pullPolicy }}
          command:
            - sh
            - -ec
            - curl -sS http://{{ include "ret2shell.victoriaLogsName" . }}:{{ .Values.victoriaLogs.service.port }}/ >/dev/null
{{- end }}
